import { Routes } from '@angular/router';
import { StatesInfoComponent } from './states-info/states-info.component';

export const routes: Routes = [
    {
        path: '',
        component: StatesInfoComponent
    }
];
